@model WorkoutRoutineIndexViewModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ActiveRoutine)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ActiveRoutine.Description)
            </th>
            
            <th></th>
        </tr>
    </thead>
    <tbody>
    <tr>
        <td>
            @Html.DisplayFor(modelItem => modelItem.ActiveRoutine.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => modelItem.ActiveRoutine.Description)
        </td>
        <td>
            <a asp-action="Edit" asp-route-id="@Model.ActiveRoutine.Id">Edit</a> |
            <a asp-action="Details" asp-route-id="@Model.ActiveRoutine.Id">Details</a> |
            <a asp-action="Delete" asp-route-id="@Model.ActiveRoutine.Id">Delete</a>
        </td>
        </tr>
}
    </tbody>
</table>
